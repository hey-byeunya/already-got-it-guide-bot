.sources/
.models/
# 심볼릭 링크로 만든 모델 캐시(support-bot/.models → ../dev-bot/.models)까지 막는다.
# `.models/` 는 디렉터리만 막아서 링크가 통과했다 — 이 컴퓨터에만 맞는 경로다.
.models
node_modules/
*/app/node_modules/
*/app/dist/
*.tsbuildinfo
TODO.md
.DS_Store
.deploy/

# 파이썬 바이트코드 — build_chunks.cpython-314.pyc 가 실제로 커밋돼 있었다.
# 파이썬 버전이 바뀌면 파일 이름까지 바뀌어 계속 새 잡음이 된다.
__pycache__/
*.pyc

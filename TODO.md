# 5강 · 나만의 챗봇 서비스 만들기 — To-Do

> RAG 가이드 챗봇을 PRD부터 배포·제출까지 이어가는 실행 체크리스트입니다.
> 순서대로 진행하세요 — **A.PRD → B.자료수집 → C.벡터스토어 → D.검색 → E.프롬프트·UI → F.판정 → G.실험 → H.제출**
> 실제 코딩 프로젝트 폴더(예: `my-service-agent/`)에 이 파일을 그대로 옮겨 두고 체크해 가며 쓰면 됩니다.

---

## A. PRD 작성

자기 도메인 선정 · 여덟 질문 · 목표/비목표/수용 기준

- [x] 공개 URL로 자료를 확인할 수 있는 도메인을 하나 정했다
- [x] 여덟 질문에 한 문장씩 답했다 (모르면 `[결정 필요]`로 남김)
- [x] 답을 목표·비목표·수용 기준 세 제목 아래로 재배열했다
- [x] 수용 기준을 실제 입력할 질문과 관찰할 화면 상태로 바꿔 썼다
- [x] PRD 검토 프롬프트를 에이전트에게 보내 모순·빠진 기준을 확인받았다 (코드 생성은 아직 요청하지 않음)

### 스스로에게 묻는 여덟 질문

1. 누구의 어떤 불편을 다룹니까
2. 사용자가 끝내 할 수 있어야 합니까
3. 제품의 한 문장 목표는 무엇입니까
4. 이번 범위에서 하지 않을 일은 무엇입니까
5. 어떤 자료를 사실로 삼습니까
6. 답하지 못할 때 어떤 행동을 합니까
7. 사용자가 성공을 어떻게 알아봅니까
8. 어떤 관찰로 수용 여부를 판정합니까

### PRD 검토 프롬프트

```text
당신은 브라우저 기반 RAG 안내 챗봇의 제품 설계 검토자입니다.
아래 PRD 초안을 읽고 구현을 시작하지 말고 먼저 검토합니다.

도메인: [내 도메인]
목표: [사용자가 할 수 있어야 하는 일]
비목표: [이번 범위에서 하지 않을 일]
공개 자료 후보: [URL 또는 자료 종류]
수용 기준: [질문과 관찰 가능한 결과]

반드시 다음 순서로 답합니다.
1. 목표와 비목표가 충돌하거나 모호한 지점을 질문으로 제시합니다.
2. 공개 자료만으로 검증할 수 없는 수용 기준을 표시합니다.
3. 도메인 밖 질문, 근거 부족, 판정 실패의 처리 기준을 제안합니다.
4. 수정된 PRD 목차를 작성하되, 확인되지 않은 사실과 수치를 추가하지 않습니다.
5. 파일 생성이나 코드 작성은 제가 명시적으로 요청할 때까지 하지 않습니다.
```

### 내 PRD 초안

→ **[PRD.md](PRD.md)** 에 썼다. 이 칸에 옮겨 적으면 두 곳이 어긋난다.

- **도메인**: "이미 있어"(개인용 재고 관리 웹앱)의 이용 방법 안내
- **목표**: 공개 문서에 근거해 이용 방법을 안내하고, 근거를 화면에 함께 드러낸다
- **비목표**: 로그인·개인 데이터 조회·값 판정·상태 변경 제안·계정 조작 대행·서버 수집·다국어·모바일 최적화
- **수용 기준**: 6개 — [PRD.md 7절](PRD.md#7-수용-기준)
- **검토 결과**: [PRD.md 10절](PRD.md#10-prd-검토-결과) — 지적 6건과 대응
- **남은 `[결정 필요]`**: 6곳 — [PRD.md 9절](PRD.md#9-결정-필요--아직-정하지-못한-것)

---

## B. 자료 수집 — 8~15청크

공개 원문 → 최소 120자 사실 단위 → `id`·`text`·`url`·`section`

- [x] 자기 도메인의 공개 원문 후보를 찾고 URL·주제를 기록했다
- [x] 로그인·권한이 필요한 자료는 공개 자료 후보와 구분해 두었다
- [x] 각 자료를 최소 120자 이상의 독립된 사실 단위로 나눴다
- [x] 일정한 접두어로 임시 ID를 부여했다 (예: `MC-001`)
- [x] 8~15개 청크에 id, text, url, section을 채웠다
- [x] text만 읽어도 질문과 관계를 판단할 수 있는지 교차 검토했다
- [x] URL 없는 행, 두 주제가 섞인 행, 너무 짧은 행을 찾아 고쳤다

### 자료 수집 위임 프롬프트

```text
당신은 공개 자료 기반 RAG 코퍼스의 수집 보조자입니다.
도메인: [내 도메인]
자료 범위: [확인할 공개 자료 범위]
공개 자료 후보: [확인할 URL 목록]

다음 원칙으로 8~15개 청크 초안을 제안합니다.
1. 각 청크는 최소 120자의 독립된 사실 단위로 제안합니다.
2. id, text, url, section 필드를 모두 제시합니다.
3. URL을 실제로 확인하지 못했으면 추정하지 말고 [검증 필요]로 표시합니다.
4. 한 청크에 서로 다른 주제를 섞지 않습니다.
5. 원문에 없는 평가, 날짜, 수치, 관계를 보태지 않습니다.
6. 마지막에 중복 가능 청크와 사람이 원문으로 확인할 항목을 목록으로 정리합니다.
```

### 청크 초안 (8~15행)

→ **14개.** 데이터는 [`data/chunks.json`](data/chunks.json), 수집·검증 기록은 [`docs/corpus.md`](docs/corpus.md).

- 원문에서 **연속 구간을 그대로** 잘랐다. 요약하거나 다시 쓰지 않았다
- 잘라낸 본문이 원문에 그대로 있는지 14/14 대조 통과 (`scripts/build_chunks.py`가 매번 확인)
- 모든 청크 120자 이상, 앵커까지 붙은 URL이 실제로 열리는지 14/14 확인
- 출처별: PRD 8 · CLAUDE 3 · README 2 · migration.sql 1

---

## C. 벡터스토어 구축

`embeddinggemma-300m-ONNX` · q4 · mean pooling · 768차원

- [x] `embed-docs-browser-path.mjs`로 청크 배열을 읽어 벡터를 생성했다
- [x] `dtype:"q4"`, `pooling:"mean"`, `normalize:true`로 고정했다
- [x] id, text, url, section을 보존하고 vector를 추가했다
- [x] 결과를 `app/public/(내파일명).json`에 저장했다
- [x] 모든 vector가 768차원인지 검사하는 검증을 포함했다
- [x] 과거 384차원(BGE) 산출물을 새 파일에 섞지 않았다
- [x] `check_retrieval.py`로 예상 질문의 top-1·top-3을 확인했다

### 벡터스토어 생성 요청

```text
[내 도메인] 공개 문서 청크 배열을 읽어 브라우저 RAG용 벡터스토어를 만드는
embed-docs-browser-path.mjs를 작성합니다. onnx-community/embeddinggemma-300m-ONNX를
사용하고 dtype은 q4, pooling은 mean, normalize는 true로 고정합니다.
각 청크의 id, text, url, section을 보존하고 vector를 추가하여
app/public/(내파일명).json에 저장합니다. 기존 BGE 384차원 경로는 사용하지 말고,
생성 후 모든 vector가 768차원인지 검사하는 검증도 포함합니다.
```

### 검색 스팟체크

→ 질문 14개(근거 있는 것 10 · 범위 밖 4). 전체 기록은 [`docs/vectorstore.md`](docs/vectorstore.md), 원자료는 [`data/spotcheck-result.json`](data/spotcheck-result.json).

| 지표 | 결과 |
|---|---|
| 근거 있는 질문 top-1 일치 | 8/10 |
| 근거 있는 질문 top-3 포함 | **10/10** |
| 범위 밖 질문이 0.55 미만 | **0/4** |

⚠️ **0.55 임계가 이 자료에서는 작동하지 않는다.** 근거 있는 질문 0.633~0.792 / 범위 밖 0.584~0.648 로 두 무리가 겹친다(틈 −0.015). "내일 날씨 어때?"가 0.584 라 약한 근거 경고가 켜지지 않는다. 지금 고치지 않고 G 의 실험 후보로 넘겼다.

---

## D. 하이브리드 검색 구현

코사인 상위 10 + BM25 미중복 상위 5 · 0.55 약한 근거 임계

- [x] 코사인 유사도 상위 10개를 먼저 고르는 함수를 구현했다
- [x] 중복되지 않은 BM25 상위 5개를 추가했다
- [x] BM25 점수를 해당 검색의 최고 점수로 나눠 0~1로 정규화했다
- [x] 반환값마다 chunk, score, method(vector/bm25)를 보존했다
- [x] 최고 유사도가 0.55 미만이면 결과를 버리지 않고 약한 근거 상태를 반환했다
- [x] 고유명사·정확한 표기가 필요한 질문으로 BM25 보조 효과를 확인했다

→ 구현은 [`app/src/lib/search.ts`](app/src/lib/search.ts), 확인 기록은 [`docs/search.md`](docs/search.md).

**확인 결과 요약**

| 확인 | 결과 |
|---|---|
| 정확한 표기 질문 4개 1위 일치 | 코사인 2 · BM25 2 — **BM25 가 코사인을 이기지 못했다** |
| BM25 가 새로 넣을 수 있는 자리 | 최대 4개 (청크 14개 − 코사인 상위 10) |
| 질문당 근거로 넘어간 청크 수 | 10~13개 — 검색이 거의 걸러 내지 않는다 |
| 약한 근거로 잡힌 질문 | 0/18 |

⚠️ **BM25 가 범위 밖 질문에 근거를 만들어 준다.** "파이썬으로 웹 크롤링하는 방법 알려줘" 에 AG-001 이 정규화 점수 **1.00** 으로 붙는다. 겹친 것은 "방법" 같은 흔한 조각뿐이다. 최고 점수로 나누는 정규화에서 1.00 은 "관련 있다" 가 아니라 "이 검색 안에서 1등" 이라는 뜻이다.

### 검색 합성 요청

```text
[내 도메인] 벡터스토어 검색 함수를 구현합니다. 질문 임베딩과 문서 벡터의
코사인 유사도로 상위 10개를 고른 뒤, 중복되지 않은 BM25 상위 5개를 추가합니다.
BM25 점수는 해당 검색의 최고 점수로 나누어 0~1로 정규화합니다.
반환값마다 chunk, score, method("vector" 또는 "bm25")를 보존하고,
최고 유사도가 0.55보다 낮으면 결과를 버리지 말고 약한 근거 상태를 반환합니다.
```

---

## E. 프롬프트 조립 + UI 스트리밍

`buildPrompt()` · Ollama `/api/chat` 스트리밍 · `OLLAMA_ORIGINS`

- [x] buildPrompt()가 소개→근거 원칙→[ID] 표시 요구→KST 시각→자료→질문 순서로 조립되게 했다
- [x] 약한 근거(0.55 미만)일 때의 보수화 문구를 추가했다
- [x] POST /api/chat에 stream:true, think:false를 포함했다
- [x] ReadableStream을 줄 단위로 파싱해 화면에 이어 붙였다
- [x] AbortSignal로 생성 중지 버튼을 구현했다
- [x] GET /api/tags로 연결 상태를 확인하고 배너·재시도를 붙였다
- [x] OLLAMA_ORIGINS를 내 배포 주소로 설정하고 앱을 재시작했다
- [x] 출처 칩(ID·section·방법·점수)이 표시되는지 확인했다

→ 앱은 [`app/`](app), 확인 기록은 [`docs/ui.md`](docs/ui.md).

**확인 결과 요약**

| 확인 | 결과 |
|---|---|
| 브라우저와 Node 임베딩이 같은 공간인가 | **코사인 1.000000** — C 에서 남긴 숙제 해결 |
| 사용 조건 4단계 = README | 스크립트 대조 **4/4 같음** (수용 기준 6번 통과) |
| 중지 버튼 | 멈추고 이미 받은 답과 출처가 그대로 남음 |
| 출처 칩 | `AG-004 · 주요 기능 — 위시 · vector · 0.699` + 원문 링크 |
| 약한 근거 문구 | 구현했지만 **한 번도 들어가지 않았다** (0.55 아래로 내려가는 질문이 없다) |

⚠️ **프롬프트는 맞는데 답이 틀렸다.** 수용 기준 1번 질문에서 AG-004 가 첫 번째 근거로 들어가고 그 안에 답이 그대로 있는데도, 모델은 "문서에 없다" 고 답하고는 서버 RPC 절차를 지어냈다. `[ID]` 도 요구한 `[AG-004]` 가 아니라 자료 블록의 표기 `[AG-005 | 섹션]` 를 따라 했다. 지금 고치지 않고 G 후보 7·8·9 로 넘겼다.

⚠️ `OLLAMA_ORIGINS` 는 설정했지만 **Ollama 재시작이 남았다** — 지금 도는 프로세스는 옛 설정으로 떠 있다.

### UI 골격 요청

```text
Vite, React, TypeScript로 [내 도메인] RAG 챗봇 UI 골격을 만듭니다.
로컬 qwen3.5:2b 엔진 선택, GET /api/tags 연결 상태 배너와 재시도,
임베딩·하이브리드 검색·생성 단계 표시, 스트리밍 답변과 AbortSignal 중지 버튼을 구현합니다.
각 답변에는 출처 칩을 표시하고 칩에는 ID, section, vector 또는 bm25 방법, 점수를 보입니다.
최고 유사도가 0.55보다 낮으면 약한 근거 경고를 보이며, 스트리밍 오류가 나도
이미 받은 답변과 출처는 유지합니다. Chrome과 Edge 권장, Safari 불안정 안내도 포함합니다.
```

### 참고 — OLLAMA_ORIGINS 설정

```bash
# macOS
launchctl setenv OLLAMA_ORIGINS "https://YOUR_ID.github.io"

# Windows
setx OLLAMA_ORIGINS "https://YOUR_ID.github.io"

# systemd 환경
systemctl set-environment OLLAMA_ORIGINS="https://YOUR_ID.github.io"
```

설정 뒤에는 Ollama를 재시작해야 합니다.

---

## F. LLM-as-a-Judge 리허설

무근거 질문("내일 날씨 어때?")으로 `refusal:true` 확인

- [ ] judge 프롬프트를 구현했다 (grounded/noHalluc/cited/refusal/score/comment)
- [ ] stream:false, think:false, format:"json", temperature:0으로 로컬 judge를 호출했다
- [ ] score가 5점 만점처럼 나오면 (score/5)×100으로 환산하는 방어 로직을 넣었다
- [ ] judge 실패(JSON 파싱 실패 등) 시 judgeError 배지만 남기고 UI를 유지하게 했다
- [ ] "내일 날씨 어때?"를 입력해 refusal:true를 확인했다
- [ ] 질문 원문·답변 핵심 문장·판정 배지·평어를 함께 기록했다

### Judge 프롬프트

```text
당신은 RAG 챗봇 답변의 평가자입니다. 아래 [질문], [근거자료], [답변]을 읽고 다음 기준으로 JSON만 출력합니다.
grounded: 답변 내용이 근거자료에서 나왔는가 (true/false)
noHalluc: 근거에 없는 사실을 지어내지 않았는가 (true/false)
cited: 답변 안에 근거 조각의 [ID] 표시가 있는가 (true/false)
refusal: 근거에 답이 없어서 '없다'고 답한 경우 true, 그 외 false
score: 0-100 정수 (grounded·noHalluc·cited 반영)
comment: 한두 문장 평어 (한국어)
출력 형식: {"grounded":bool,"noHalluc":bool,"cited":bool,"refusal":bool,"score":int,"comment":"..."} — JSON 외 텍스트 금지.

[질문] …
[근거자료] …
[답변] …
```

### 무근거 질문 리허설 기록

| 항목 | 기록 |
|---|---|
| 입력한 질문 | 내일 날씨 어때? |
| 검색 결과가 비었는가 | |
| 답변이 자료 밖 사실을 지어냈는가 | |
| 판정 배지 (grounded/noHalluc/cited/refusal/score) | |
| comment 평어 | |

---

## G. 루브릭 실험

한 번에 하나만 바꾼다 · Q1~Q9 고정 질문 · README.md 기록

- [ ] 중요한 지표 3~5개를 고르고 각 지표가 어떤 judge 필드와 연결되는지 적었다
- [ ] 정상·경계·무근거 질문을 각 최소 3개씩(Q1~Q9) 고정했다
- [ ] 현재 세팅으로 전 질문을 실행해 답변·근거·판정·루브릭 스코어를 기록했다 (기준 행)
- [ ] 루브릭 지표와 관계있는 변수 하나만 골라 변경했다
- [ ] 같은 질문 세트·같은 순서로 재실행하고 judge 필드를 모았다
- [ ] 기대와 다른 답변도 지우지 않고 다음 결정을 한 줄로 남겼다
- [ ] README.md에 실험 이름·바꾼 변수·고정 질문·판정 필드·채택 또는 되돌림 결정을 기록했다

> ⚠️ **한 번에 하나만 바꾼다** — 검색 결과 수와 프롬프트의 길이 지시를 동시에 고치면, 어느 변경이 grounded·cited·답변 길이에 영향을 주었는지 분리할 수 없습니다. 바꿀 수 있는 축은 둘입니다 — **검색 축**(top-k, 0.55 미만일 때의 약한 근거 문구, 벡터·BM25 구성)과 **프롬프트 축**(근거 원칙 문구, 답변 길이 지시, [ID] 표시 규칙, 시간 컨텍스트 문구).

### 지표표

| 지표 | 측정 방법 | 목표값 | 지금 값 |
|---|---|---|---|
| 출처를 인용한 답변 | 고정 질문의 cited:true 답 수 | | |
| 도메인 밖 질문의 거부 | 무근거 질문의 refusal:true 답 수 | | |
| 근거성 | 판정 배지의 grounded:true 비율 | | |
| 짧은 답변의 근거 유지 | 답변 길이와 [ID] 표시를 함께 검토 | | |

### 고정 질문 세트 (Q1~Q9)

| 유형 | 질문 |
|---|---|
| 정상 (Q1) | |
| 정상 (Q2) | |
| 정상 (Q3) | |
| 경계 (Q4) | |
| 경계 (Q5) | |
| 경계 (Q6) | |
| 무근거 (Q7) | |
| 무근거 (Q8) | |
| 무근거 (Q9) | |

### 실험 기록 — 기준 행 vs 변경 행

| 실험 | 바꾼 변수 | cited 수 | refusal 수 | grounded 비율 | 채택/되돌림 |
|---|---|---|---|---|---|
| 기준 (현재 세팅) | — | | | | |
| 실험 1 | | | | | |
| 실험 2 | | | | | |

### 실험 설계 검토 프롬프트

```text
내 루브릭과 질문 세트를 읽고, 한 번에 하나의 변수만 바꾸는 실험 순서를 제안해 줘.
변경이 루브릭의 어느 지표에 영향을 줄지 예측도 함께. 측정되지 않은 예측은 [추정]으로 표시해 줘.
```

---

## H. 최종 제출

설계 → 구현 → 실험 → 배포 → 제출

- [ ] PRD의 모든 `[결정 필요]`를 채워 완성본으로 닫았다
- [ ] 스키마·검색·프롬프트 파이프라인은 그대로 두고 청크·소개 문구만 내 도메인으로 교체했다
- [ ] 임베딩을 embeddinggemma-300m 768차원으로 통일했다
- [ ] 루브릭을 새 챗봇에 재적용해 고정 질문 세트로 확인했다
- [ ] `npm run build` 후 `dist`를 GitHub Pages에 배포했다 (`base:'./'`)
- [ ] 배포 주소에서 OLLAMA_ORIGINS 설정 뒤 소개 질문·도메인 밖 질문을 각각 확인했다
- [ ] Ollama 연결 실패 시 안내와 재시도가 보이는지 확인했다
- [ ] README.md에 소개·설계 결정 요약·실험 결과·배포 주소·사용 조건을 적었다
- [ ] 저장소에 README.md, PRD.md, 코드, 배포 주소를 함께 두었다

### 구현 교체 요청

```text
스키마와 검색·프롬프트 파이프라인은 변경하지 마세요. 모두콘 예시 코드에서
청크 데이터와 소개·페이지 문구를 [내 도메인]으로 교체해 주세요. 청크는
[자료 경로]를 근거로 만들고 id, text, url, section, vector 필드를 유지하세요.
임베딩은 embeddinggemma-300m의 768차원으로 통일하고,
[도메인 밖 질문 처리 기준]을 문구에 반영하세요.
```

### 제출 정보

- **저장소 URL**:
- **배포 주소**:
- **사용 조건 요약** (Ollama 실행, 임베딩 다운로드 용량, CORS 설정):
- **이번 프로젝트에서 실행하지 않은 것과 그 이유**:

---

## 막힌 점 / 확인이 필요한 것

>
